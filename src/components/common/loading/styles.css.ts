import { style } from 'styles';

export const loadingStyles = {
  container: style({
    display: 'flex',
    height: '100%',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  }),
};
