import { style } from 'styles';

export const styles = {
  container: style({
    display: 'flex',
    height: '100%',
    width: '100%',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  }),
  content: style({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  }),
};
